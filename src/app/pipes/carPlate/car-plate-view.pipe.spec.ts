import { CarPlateViewPipe } from './car-plate-view.pipe';

describe('CarPlateViewPipe', () => {
  it('create an instance', () => {
    const pipe = new CarPlateViewPipe();
    expect(pipe).toBeTruthy();
  });
});
