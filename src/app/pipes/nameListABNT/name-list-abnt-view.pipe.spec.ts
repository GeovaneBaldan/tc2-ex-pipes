import { NameListAbntViewPipe } from './name-list-abnt-view.pipe';

describe('NameListAbntViewPipe', () => {
  it('create an instance', () => {
    const pipe = new NameListAbntViewPipe();
    expect(pipe).toBeTruthy();
  });
});
